{
  "name": "@kumooo/api",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply kumooo-hosting --local",
    "db:migrate": "wrangler d1 migrations apply kumooo-hosting --remote",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"ok\""
  },
  "dependencies": {
    "@kumooo/plans": "workspace:*",
    "hono": "^4.7.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.113.0"
  }
}
