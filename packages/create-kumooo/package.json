{
  "name": "create-kumooo",
  "version": "0.2.0",
  "description": "Create a kumooo.js app (blank, blog, or shop)",
  "type": "module",
  "license": "MIT",
  "bin": {
    "create-kumooo": "./dist/index.js"
  },
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/sync-templates.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"ok\"",
    "prepack": "pnpm build"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
