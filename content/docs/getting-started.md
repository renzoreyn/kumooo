# Getting started

```bash
npx create-kumooo
```

Choose **blank**, **blog**, or **shop**. Then:

```bash
cd your-app
pnpm install
pnpm dev
```

That is the whole on-ramp. Edit `app/page.tsx`. Keep `@kumooo/ui`.
